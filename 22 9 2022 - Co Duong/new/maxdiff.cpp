#include<bits/stdc++.h>

using namespace std;
const long long MAX = 1e5 + 5;
const long long N = 20;

int dp[N][1000];
int n,p,q,sum;

int calc1(int id,int s){
	if(id == n)
}

void solve(){
	
	if(p > q)swap(p,q);
	
	
}

signed main(){
	
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);
	
	if(ifstream("MAXDIFF.INP")){
		freopen("MAXDIFF.INP","r",stdin);
		freopen("MAXDIFF.OUT","w",stdout);
	}
	
	int t;
	cin>>t;
	while(t--){
		cin>>n>>p>>q;
		solve();
	}
	
}